numeros = ''
pipeline{
  agent any
  stages{
    stage(''){
      steps{
        script{
           numeros = readFile(file: "\\numeros.txt", '${numero1}')
            println 'La variable es: ' + numeros
            //println 'Prueba: ' + numeros.toInteger()*numeros.toInteger()
        }
      }
    }
  }
}
